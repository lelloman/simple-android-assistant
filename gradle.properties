org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# The extracted modules still use the Kotlin Gradle plugin and legacy Android
# DSL. Keep them compatible with their Pezzottify source until the first API is
# stabilized, then migrate the standalone build to AGP's built-in Kotlin.
android.builtInKotlin=false
android.newDsl=false
